{
  "name": "dashboard-state-aggregates-widget",
  "version": "1.0.0",
  "type": "module",
  "description": "CloudWatch custom widget for displaying workflow phase aggregates from DynamoDB",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "tsc --pretty false --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.678.0",
    "@aws-sdk/lib-dynamodb": "^3.678.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.16.3",
    "esbuild": "^0.27.0",
    "typescript": "^5.7.2"
  }
}
