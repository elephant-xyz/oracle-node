From 14c576e79e8feb307b519c7a71b663a2c7250f1a Mon Sep 17 00:00:00 2001
From: Cursor Agent <cursoragent@cursor.com>
Date: Sat, 29 Aug 2026 17:14:15 +0000
Subject: [PATCH] Fix Broward live capture transforms

---
 broward/scripts/data_extractor.js             | 41 +++++++++-------
 broward/scripts/findBrowardPropertyMapping.js | 48 +++++++++++++++++++
 broward/scripts/layoutMapping.js              | 13 +++--
 broward/scripts/ownerMapping.js               | 11 +++++
 broward/scripts/structureMapping.js           | 12 +++--
 broward/scripts/utilityMapping.js             | 13 ++---
 6 files changed, 110 insertions(+), 28 deletions(-)
 create mode 100644 broward/scripts/findBrowardPropertyMapping.js

diff --git a/broward/scripts/data_extractor.js b/broward/scripts/data_extractor.js
index 5dcf0ef..ecc60ea 100644
--- a/broward/scripts/data_extractor.js
+++ b/broward/scripts/data_extractor.js
@@ -4722,10 +4722,13 @@ function parseCoordinate(value) {
 }
 
 const appendSourceInfo = (seed) => ({
-  source_http_request: {
-    method: "GET",
-    url: seed?.source_http_request?.url || null,
-  },
+  source_http_request:
+    seed?.source_http_request && typeof seed.source_http_request === "object"
+      ? { ...seed.source_http_request }
+      : {
+          method: "POST",
+          url: null,
+        },
   request_identifier: seed?.request_identifier || seed?.parcel_id || "",
 });
 
@@ -5246,7 +5249,8 @@ function extractMailingAddress(inputObj) {
   );
   const layoutPath = path.join(process.cwd(), "owners", "layout_data.json");
 
-  const input = readJson(inputPath);
+  const capturedInput = readJson(inputPath);
+  const input = capturedInput?.input?.response || capturedInput;
   const unAddr = readJson(addrPath);
   const seed = readJson(seedPath);
   const ownerData = readJson(ownerPath);
@@ -5259,8 +5263,7 @@ function extractMailingAddress(inputObj) {
       ? d.parcelInfok__BackingField[0]
       : null;
   if (!parcelInfo) {
-    console.error("No parcel info found in input.json");
-    process.exit(0);
+    errorOut("No parcel info found in input.json", "input.d.parcelInfok__BackingField");
   }
 
   const parcelId = parcelInfo.folioNumber || null;
@@ -5269,14 +5272,12 @@ function extractMailingAddress(inputObj) {
   const underAir = parcelInfo.bldgUnderAirFootage != null && String(parcelInfo.bldgUnderAirFootage).trim() !== "" ? String(parcelInfo.bldgUnderAirFootage).trim() : null;
   const livable = (() => { let l = underAir; if (!l && parcelInfo.bldgSqFT != null) { const s = String(parcelInfo.bldgSqFT).trim(); if (s && s !== "") l = s; } if (l != null) { const n = parseFloat(l); if (!isNaN(n) && n <= 0) l = null; } return l; })();
   // Find matching property mapping by use code
+  const { findBrowardPropertyMapping } = require("./findBrowardPropertyMapping.js");
   const useCode = (parcelInfo.useCode || "").trim();
-  const propertyMapping = propertyUseCodeMappings.find(mapping => {
-    const mappingCode = mapping.property_usecode.split(' ')[0]; // Extract code part (e.g., "01-01")
-    return mappingCode === useCode || mapping.property_usecode.startsWith(useCode);
-  }) || propertyUseCodeMappings.find(mapping => 
-    mapping.property_usecode.toLowerCase().includes(useCode.toLowerCase())
-  );
-  
+  const propertyMapping = findBrowardPropertyMapping(useCode, propertyUseCodeMappings);
+  if (!propertyMapping) {
+    errorOut("Unmapped Broward useCode: " + useCode, "property.property_usage_type");
+  }
   const propertyFields = {
     property_type: propertyMapping.property_type,
     property_usage_type: propertyMapping.property_usage_type,
@@ -5398,9 +5399,9 @@ function extractMailingAddress(inputObj) {
     if (isFinite(value) && value > 0) {
       // Convert based on land calculation type
       if (type === "SQUARE FOOT" || type === "OUTDOOR ADVERTISING") {
-        lotSqft = value;
+        lotSqft = Math.round(value);
       } else if (["ACREAGE", "AGRICULTURE", "AIR RIGHTS", "BUMBLE BEES", "CATTLE", "FISH FARMING", "CITRUS", "GOLF COURSE", "GOAT FARM", "HORSE BUSINESS", "MISCELLANEOUS AGRICULTURE", "NURSERY", "POULTRY FARM", "ROW CROPS", "ROCK PIT", "SHEEP FARM"].includes(type)) {
-        lotSqft = value * 43560; // Convert acres to square feet
+        lotSqft = Math.round(value * 43560); // Convert acres to square feet
       } else if (type === "FRONT FOOT FACTOR") {
         lotSqft = null; // Cannot convert front foot to square feet without depth
       } else if (type === "FLAT VALUE" || type === "CEMETARY LOTS") {
@@ -5662,6 +5663,10 @@ function extractMailingAddress(inputObj) {
   if (utilSection) {
     const utility = { ...appendSourceInfo(seed), ...utilSection };
     writeJson(path.join(dataDir, "utility.json"), utility);
+    writeJson(path.join(dataDir, "relationship_property_has_utility.json"), {
+      from: { "/": "./property.json" },
+      to: { "/": "./utility.json" },
+    });
   }
 
   // LAYOUTS
@@ -5732,4 +5737,8 @@ function extractMailingAddress(inputObj) {
     structural_damage_indicators: null,
   };
   writeJson(path.join(dataDir, "structure.json"), structure);
+  writeJson(path.join(dataDir, "relationship_property_has_structure.json"), {
+    from: { "/": "./property.json" },
+    to: { "/": "./structure.json" },
+  });
 })();
diff --git a/broward/scripts/findBrowardPropertyMapping.js b/broward/scripts/findBrowardPropertyMapping.js
new file mode 100644
index 0000000..c62f3be
--- /dev/null
+++ b/broward/scripts/findBrowardPropertyMapping.js
@@ -0,0 +1,48 @@
+"use strict";
+
+function mappingUseCodeToken(mappingLabel) {
+  return mappingLabel.trim().split(/\s+/u)[0] || "";
+}
+
+function parseBrowardUseCodePrefix(useCode) {
+  const compact = useCode.trim().toUpperCase();
+  const subtypeMatch = compact.match(/^(\d{2}-\d{2})\b/u);
+  const familyMatch = compact.match(/^(\d{2})(?!\d)/u);
+  return {
+    family: familyMatch ? familyMatch[1] : undefined,
+    subtype: subtypeMatch ? subtypeMatch[1] : undefined,
+  };
+}
+
+function findBrowardPropertyMapping(useCode, mappings) {
+  const trimmed = String(useCode || "").trim();
+  if (!trimmed) return undefined;
+  const parsed = parseBrowardUseCodePrefix(trimmed);
+  if (parsed.subtype) {
+    const exact = mappings.find(function (mapping) {
+      return mappingUseCodeToken(mapping.property_usecode) === parsed.subtype;
+    });
+    if (exact) return exact;
+  }
+  const lower = trimmed.toLowerCase();
+  const includesHit = mappings.find(function (mapping) {
+    return mapping.property_usecode.toLowerCase().includes(lower);
+  });
+  if (includesHit) return includesHit;
+  if (!parsed.family) return undefined;
+  const preferred = mappings.find(function (mapping) {
+    return mappingUseCodeToken(mapping.property_usecode) === parsed.family + "-01";
+  });
+  if (preferred) return preferred;
+  return mappings.find(function (mapping) {
+    return mappingUseCodeToken(mapping.property_usecode).startsWith(
+      parsed.family + "-",
+    );
+  });
+}
+
+module.exports = {
+  findBrowardPropertyMapping: findBrowardPropertyMapping,
+  mappingUseCodeToken: mappingUseCodeToken,
+  parseBrowardUseCodePrefix: parseBrowardUseCodePrefix,
+};
diff --git a/broward/scripts/layoutMapping.js b/broward/scripts/layoutMapping.js
index 9919837..a27656b 100644
--- a/broward/scripts/layoutMapping.js
+++ b/broward/scripts/layoutMapping.js
@@ -120,9 +120,16 @@ function createBathroomLayouts(counts) {
 }
 
 function extractFromInput(inputObj) {
-  const root = inputObj.d || inputObj;
-  const parcelArr = root.parcelInfok__BackingField || [];
-  const parcel = parcelArr[0] || {};
+  const data =
+    inputObj && inputObj.input && inputObj.input.response
+      ? inputObj.input.response
+      : inputObj;
+  const root = data.d || data;
+  const parcelArr = root.parcelInfok__BackingField;
+  if (!Array.isArray(parcelArr) || parcelArr.length === 0) {
+    throw new Error("No Broward parcel records found in input.json");
+  }
+  const parcel = parcelArr[0];
   const folio = parcel.folioNumber || parcel.folio || "unknown";
 
   const beds = coerceInteger(parcel.beds) || 0;
diff --git a/broward/scripts/ownerMapping.js b/broward/scripts/ownerMapping.js
index cb292b7..7aa4e9c 100644
--- a/broward/scripts/ownerMapping.js
+++ b/broward/scripts/ownerMapping.js
@@ -432,6 +432,17 @@ let parsed = null;
 if (pageText.trim().startsWith("{")) {
   parsed = JSON.parse(pageText);
 }
+if (parsed && parsed.input && parsed.input.response) {
+  parsed = parsed.input.response;
+}
+if (
+  !parsed ||
+  !parsed.d ||
+  !Array.isArray(parsed.d.parcelInfok__BackingField) ||
+  parsed.d.parcelInfok__BackingField.length === 0
+) {
+  throw new Error("No Broward parcel records found in input.json");
+}
 
 // Collect owner name candidates and property id from parsed object if available
 const ownerCandidates = [];
diff --git a/broward/scripts/structureMapping.js b/broward/scripts/structureMapping.js
index 353634f..ef54439 100644
--- a/broward/scripts/structureMapping.js
+++ b/broward/scripts/structureMapping.js
@@ -15,13 +15,19 @@ function safeParseJSON(str) {
 
 function extractFromInput(inputObj) {
   // The provided input is JSON; handle case if wrapped/HTML by attempting cheerio when needed
-  let data = inputObj;
+  let data =
+    inputObj && inputObj.input && inputObj.input.response
+      ? inputObj.input.response
+      : inputObj;
   if (!data || typeof data !== "object") {
     return null;
   }
   const root = data.d || data;
-  const parcelArr = root.parcelInfok__BackingField || [];
-  const parcel = parcelArr[0] || {};
+  const parcelArr = root.parcelInfok__BackingField;
+  if (!Array.isArray(parcelArr) || parcelArr.length === 0) {
+    throw new Error("No Broward parcel records found in input.json");
+  }
+  const parcel = parcelArr[0];
 
   const folio = parcel.folioNumber || parcel.folio || "unknown";
   const useCode = parcel.useCode || "";
diff --git a/broward/scripts/utilityMapping.js b/broward/scripts/utilityMapping.js
index 0cebe0c..f5d15df 100644
--- a/broward/scripts/utilityMapping.js
+++ b/broward/scripts/utilityMapping.js
@@ -22,12 +22,13 @@ function ensureFile(fp, dataObj) {
     process.exit(1);
   }
 
-  const parcel =
-    (data &&
-      data.d &&
-      Array.isArray(data.d.parcelInfok__BackingField) &&
-      data.d.parcelInfok__BackingField[0]) ||
-    {};
+  const source =
+    data && data.input && data.input.response ? data.input.response : data;
+  const parcels = source && source.d && source.d.parcelInfok__BackingField;
+  if (!Array.isArray(parcels) || parcels.length === 0) {
+    throw new Error("No Broward parcel records found in input.json");
+  }
+  const parcel = parcels[0];
   const folio = parcel.folioNumber || "unknown";
 
   const utilities = {
-- 
2.43.0

